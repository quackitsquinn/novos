# Timeout in seconds that Limine will use before automatically booting.
timeout: 0

/novOS
    protocol: limine

    # Disable KASLR (it is enabled by default for relocatable kernels)
    kaslr: no

    kernel_path: boot():/boot/trampoline.bin
    module_path: boot():/boot/kernel.bin